each user in users
  .user-profile-index-page
    a(href='/#{user.username}')
      img.user-search-profile-picture(src='#{user.photo}')
    a(href='/#{user.username}')
      h5= user.name
    h6= user.tagLine
