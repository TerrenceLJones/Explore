extends ../shared/template

block content
  h1= title

  h3 Search
  form(method='GET',action='/users/search/:query')
    input(type=text, name='search', autofocus=true)
    button#filter Search

  #users
    each user in users
      h3= user.name
