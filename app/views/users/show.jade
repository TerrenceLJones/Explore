extends ../shared/template

block content
  h1= user.name

  .profile-picture(style="background-image:url('#{user.photo}')")
  span: a(href='/users/edit') Edit

  #location
    h3 Location:
      h5= user.location
  #join-date
    h3 Join Date:
    h5= moment(user.joinDate).format('MMMM Do YYYY')
  #tag-line
    h3 Bio
    h5= user.tagLine

  h3 Badges
  #badges
  h3 Current Journeys
  #current-journeys
  h3 Past Journeys
  #past-journeys
